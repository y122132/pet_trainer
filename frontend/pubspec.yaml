name: life_gotchi_frontend
description: LifeGotchi Flutter Frontend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Added dependencies
  http: ^1.1.0
  provider: ^6.0.5
  camera: ^0.10.5
  web_socket_channel: ^2.4.0
  fl_chart: ^0.66.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/characters/
