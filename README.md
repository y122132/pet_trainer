# 🐶 펫트레이너 (AI Pet Trainer)

> **내 반려동물과 함께 성장하는 AI 트레이닝 & 육성 게임**  
> 실시간 AI 비전 기술로 반려동물의 행동을 인식하고, 훈련을 통해 캐릭터를 성장시키는 인터랙티브 앱입니다.

## 📖 프로젝트 개요 (Overview)

**펫트레이너**는 단순한 게임이 아닙니다. 실제 반려동물과의 교감을 게임화(Gamification)한 프로젝트입니다.  
사용자가 스마트폰 카메라로 반려동물을 비추면, AI가 실시간으로 행동을 분석하여 훈련 성공 여부를 판단합니다.  
훈련에 성공하면 나만의 3D 캐릭터가 성장하고, **AI 페르소나**가 상황에 맞는 재치 있는 대사를 건넸니다.

---

## 🚀 주요 기능 (Key Features)

### 1. 📷 실시간 AI 훈련 (Real-time Training)
- **YOLOv11** 기반의 객체 탐지(Object Detection) 및 포즈 추정(Pose Estimation).
- **3가지 훈련 모드:**
  - **🎾 놀이 (Playing):** 강아지가 공/장난감을 가지고 노는지 인식.
  - **🥣 식사 (Feeding):** 반려동물이 밥그릇에 다가가 밥을 먹는지 인식.
  - **🤝 교감 (Interaction):** 주인과 반려동물이 함께 있는지 인식 (사람 스켈레톤 + 동물 탐지).

### 2. 💬 AI 페르소나 대화 (AI Persona)
- **LangGraph & OpenAI GPT-4o**를 활용한 동적 대화 생성.
- 단순히 "성공!"이라고만 하지 않습니다. 캐릭터의 성격과 현재 스탯(행복도, 친밀도)에 따라 **"주인님! 오늘따라 공 던지는 폼이 멋진데요?"** 같이 살아있는 반응을 보입니다.

### 3. 🏠 마이룸 & 스탯 관리 (RPG Elements)
- 훈련 보상으로 얻은 포인트로 캐릭터의 **근력, 지능, 민첩, 행복도**를 성장시킵니다.
- **레이더 차트(육각형 그래프)**를 통해 성장 밸런스를 한눈에 확인할 수 있습니다.
- 레벨업 시스템과 칭호 부여 (예: "초보 트레이너" -> "근육대장님").

---

## 🛠 기술 스택 (Tech Stack)

### Frontend (App)
- **Framework:** Flutter (Dart)
- **State Management:** Provider
- **Networking:** WebSocket (실시간 스트리밍), HTTP (REST API)
- **Charts:** fl_chart (레이더 차트 구현)
- **Camera:** Camera Plugin (실시간 프레임 캡처)

### Backend (Server & AI)
- **Framework:** Python FastAPI (Async Architecture)
- **AI Vision:** Ultralytics YOLOv11 (Object Detection & Pose Estimation)
- **AI LLM:** LangChain, LangGraph, OpenAI GPT-4o-mini
- **Database:** PostgreSQL, SQLAlchemy (Async ORM)
- **Communication:** WebSockets (이미지 스트림 처리)

---

## 📂 프로젝트 구조 (Directory Structure)

```bash
pet_trainer/
├── backend/                  # Python FastAPI 서버
│   ├── app/
│   │   ├── ai_core/          # AI 로직 (YOLO, LangGraph)
│   │   │   ├── vision/       # 이미지 분석 (detector.py)
│   │   │   └── brain/        # LLM 대화 생성 (graphs.py, prompts.py)
│   │   ├── api/              # REST API 라우터
│   │   ├── db/               # DB 모델 및 설정
│   │   ├── services/         # 비즈니스 로직 (User, Character)
│   │   ├── sockets/          # 웹소켓 핸들러 (실시간 분석)
│   │   └── main.py           # 앱 진입점
│   ├── Dockerfile            # 서버 배포용 도커 파일
│   └── requirements.txt      # 의존성 패키지
│
└── frontend/                 # Flutter 모바일 앱
    ├── lib/
    │   ├── models/           # 데이터 모델 (Character, Stat)
    │   ├── providers/        # 상태 관리 (CharProvider)
    │   ├── screens/          # UI 화면 (Home, Camera, MyRoom)
    │   ├── services/         # 통신 (SocketClient)
    │   └── main.dart         # 앱 진입점
    └── pubspec.yaml          # Flutter 패키지 설정
```

---

## ⚡ 시작하기 (Getting Started)

### 1. 백엔드 실행 (Backend)

```bash
# 1. 백엔드 폴더로 이동
cd backend

# 2. 가상환경 생성 및 활성화 (권장)
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. 의존성 설치
pip install -r requirements.txt

# 4. 환경 변수 설정 (.env 파일 생성)
# .env_example 참고하여 OPENAI_API_KEY, DB 설정 등 입력

# 5. 서버 실행
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 프론트엔드 실행 (Frontend)

```bash
# 1. 프론트엔드 폴더로 이동
cd frontend

# 2. 패키지 설치
flutter pub get

# 3. 설정 파일 확인 (lib/config.dart)
# 서버 IP 주소에 맞게 baseUrl 수정 필요

# 4. 앱 실행
flutter run
```

---

## 🚧 추후 업데이트 계획 (Roadmap)
- [ ] **배틀 시스템 (Battle Arena):** 키운 캐릭터로 다른 유저와 대결
- [ ] **다양한 동물 추가:** 앵무새, 햄스터 등 YOLO 모델 학습 강화
- [ ] **소셜 기능:** 친구의 마이룸 방문 및 방명록

---

## 👨‍💻 License
This project is for educational and portfolio purposes.
